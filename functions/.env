# Parâmetros da Cloud Function do atendente automático.
#
# Só o que NÃO é segredo mora aqui — por isso este arquivo é versionado: assim o deploy é
# reprodutível e ninguém precisa lembrar de digitar isto de novo.
#
# O IA_SECRET não entra aqui. Ele é `defineSecret` (functions/src/index.ts) e vem do
# Secret Manager: `firebase functions:secrets:set IA_SECRET`.

# Para onde a Function avisa quando chega mensagem nova.
SITE_URL=https://ocartel-497f8.web.app
