{
  "name": "ocartel-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
